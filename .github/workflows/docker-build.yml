name: Docker Build

on:
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string

jobs:
  build:
    name: Docker build test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.release_tag }}
      - name: Build and initialize production image
        env:
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          set -euo pipefail
          docker build --tag "aidle:$RELEASE_TAG" .
          docker run --rm --entrypoint sh "aidle:$RELEASE_TAG" -ec '
            DATABASE_PATH=/tmp/aidle.db node scripts/migrate.mjs
            DATABASE_PATH=/tmp/aidle.db node scripts/seed.mjs
          '
